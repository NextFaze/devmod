dist: trusty
sudo: required

language: node_js
node_js:
  - "8"
  
addons:
    chrome: stable

cache:
  directories:
     - ./node_modules

install:
  - npm install

after_success:
  - npm run coveralls